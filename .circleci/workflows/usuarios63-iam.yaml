workflows:
  iac-compute:
    jobs:
      - approval:
          name: Tech lead approvement prod 🧙‍♂️
          type: approval
          filters:
            branches:
              only: main
    jobs:
      - deploy:
          name: Deploy usuarios63-iam
          DEPLOY_ENV: dev
          TEMPLATE_FILE_NAME: template/usuarios63-iam.yaml
          RESOURCE_NAME: usuarios63-iam
          requires:
            - Tech lead approvement prod 🧙‍♂️
          filters:
            branches:
              only: main
          context:
            - idaws
            - clavaws
